# OPENCODE Instructions

Canonical policy source for this repository is ./agents/RULES.md.
Read ./agents/RULES.md in full before performing work.

Use host-managed ./playbooks/, ./references/, ./templates/, and ./scripts/ when present.
If a host-managed artifact is missing, fall back to ./agents/playbooks/, ./agents/references/, ./agents/templates/, and ./agents/scripts/.
